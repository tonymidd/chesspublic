/***按钮表现基类 */
module.exports = cc.Class({
    extends: cc.Component,

    properties: {
        /***是否自动注册
         * 默认不注册
         */
        isAutoRegisterEve:false,
    },

    // use this for initialization
    onLoad: function () {
        
    },
});
